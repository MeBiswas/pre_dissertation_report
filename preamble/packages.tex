% ============================
% PACKAGES
% ============================

% Core + encoding
\usepackage[utf8]{inputenc}
\usepackage{times}
\usepackage{ragged2e}

% Math
\usepackage{amsmath}
\usepackage{amssymb}

% Tables
\usepackage{array}
\usepackage{multirow}
\usepackage{booktabs}

% Graphics
\usepackage{graphicx}
\graphicspath{{images/}} % relative to project root (same level as main.tex)

% TikZ
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, positioning}

% Page layout etc.
\usepackage{geometry}
\usepackage{setspace}
\usepackage{titlesec}

% Captions + floats
\usepackage{caption}
\usepackage{float}

% Headers & footers
\usepackage{fancyhdr}
\usepackage{lastpage}

% Hyperlinks
\usepackage{hyperref}

% Dummy text (for now)
\usepackage{lipsum}

% Glossaries + acronyms (acronym only, NO main glossary)
\usepackage[acronym,nomain]{glossaries}

% Bibliography (biblatex + IEEE style)
\usepackage[
    backend=biber,
    style=ieee,
    sorting=nty,
    maxbibnames=5
]{biblatex}
